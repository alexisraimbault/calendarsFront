import React, {
  Component,
} from 'react';
import classNames from 'classnames';
import './styles.scss';

export default class TeamMenu extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }


  render() {
    return (
      <div>
        Team menu
      </div>
    );
  }
}
